{
    "uniqueId": "${insight.uniqueId}",
    "updatedAgreeCount": ${insight.agreeCount},
    "updatedDisagreeCount": ${insight.disagreeCount},
    "voteState": #{if agree}"agree"#{/if} #{else}"disagree"#{/else}
}
